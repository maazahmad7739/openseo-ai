{
  "name": "openseo-ai",
  "version": "0.1.0",
  "private": true,
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "prebuild": "npm --prefix apps/openseo-plus install --no-package-lock",
    "build": "npm --prefix apps/openseo-plus run build"
  }
}